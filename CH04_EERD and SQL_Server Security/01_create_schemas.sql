-- Schema:
-- A logical container/namespace used to organize database objects
-- such as tables, views, procedures, and functions.
-- A schema is inside a database, not a separate database.

CREATE SCHEMA HR;


CREATE SCHEMA Sales;
